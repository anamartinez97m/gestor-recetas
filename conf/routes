# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

POST        /recipes                controllers.RecipeManagerController.createRecipe()
POST        /ingredients            controllers.RecipeManagerController.createIngredient()

GET         /recipes                controllers.RecipeManagerController.getAllRecipes()
GET         /ingredients            controllers.RecipeManagerController.getAllIngredients()
GET         /recipes/:name           controllers.RecipeManagerController.getRecipeByName(name)


# GET /recetas -> /recetas?sabor=salado&pepito=grillo
# GET /receta/:id
# GET /ingredientes/:id
